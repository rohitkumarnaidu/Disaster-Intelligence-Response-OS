import { expect, test } from "vitest"; import { calculatePriority } from "./priority"; test("calculatePriority yields canonical output 83", () => { const result = calculatePriority("Severe", "Hospital", "High", 28.8, true, 0.55); expect(result.score).toBe(83); });
